.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.claude/
docs/PROJEKT.local.md
docs/*.local.md

# The GitHub token in use may not create or update workflow files. The CI
# definition therefore lives here untracked until the token gains the
# "workflow" scope: gh auth refresh -s workflow
.github/workflows/
